import 'package:flutter/material.dart';

//colori costanti utilizzati nel gioco
class AppColor {
  static Color primaryColor = Color(0XFF39298a);
  static Color primaryColorDark = Color(0xFF231954);
}
